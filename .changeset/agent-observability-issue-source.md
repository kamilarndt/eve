---
"eve": patch
---

Emit compact `$eve.issue` facts, including `remote_subagent` for remote agent failures, so observability clients can group issues, filter related agent runs, and deep link to the affected turn without replaying traces.

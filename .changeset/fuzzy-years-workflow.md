---
"eve": patch
---

Add configured `ExperimentalWorkflow` definitions with app-owned `load` and `advance` adapters, durable `start` and settling `stop` controls, recurring cadence support, bounded retries, latest-production iteration routing, and recursive cancellation of active local subagents. The existing bare Workflow tool opt-in remains supported.

---
"eve": patch
---

Align the `eve/nuxt` module with `eve/next`: configurable dev-server timeout (default 180s), robust loopback URL discovery, local production auto-start for preview builds, and expanded Nuxt frontend docs.

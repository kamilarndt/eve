---
"eve": patch
---

Add local Node inspector support to `eve dev` with `--inspect`, `--inspect-wait`, `--inspect-brk`, and Chrome DevTools network inspection via `--inspect-network`, plus source-map remapping so breakpoints in authored runtime files bind to the live workspace paths.

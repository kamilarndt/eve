---
"eve": patch
---

The local `eve dev` status bar now shows a gray address badge, using `:port` for `127.0.0.1`, and retains that badge as terminal width narrows. Status segments now use tighter spacing.

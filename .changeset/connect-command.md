---
"eve": patch
---

Add a `/connect` command to the `eve dev` TUI for scaffolding connections from the catalog. The task list keeps configured entries checked and directs unlinked or logged-out users to the required Vercel setup. Curated Linear connections now use its current Streamable HTTP endpoint.

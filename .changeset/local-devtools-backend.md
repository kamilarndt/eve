---
"eve": patch
---

Add an experimental opt-in local DevTools backend for `eve dev`, with loopback discovery, authenticated lifecycle/bootstrap APIs, run streaming, agent and source inspection, bounded observation events, CDP relay, logs, and supervisor-owned runtime crash state.

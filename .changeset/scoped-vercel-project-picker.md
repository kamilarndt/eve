---
"eve": patch
---

Search every Vercel project in the selected team from the existing-project picker. Project linking now writes and verifies the selected `.vercel/project.json` identity without invoking Vercel CLI plugin onboarding.
